name: First Workflow

on:
  push:
    branches:
      - master
      - dev
      # - 'release/**'

jobs:
  example-job:
    runs-on: Linux

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Echo Hello, World!
        run: echo "Hello, World!"